checks:
    php: true
filter: {  }
coding_style:
    php: {  }

build:
    tests:
        override:
            -
                command: 'phpunit --coverage-clover=coverage'
                coverage:
                    file: 'coverage'
                    format: 'clover'
